version: '3.8'

services:
  smartcms-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartcms-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
